[pytest]
addopts = -ra
markers =
    slow: "expensive robustness/perf tests"
    obs: "observability smoke"
    needs_numpy: "requires NumPy"
filterwarnings =
    ignore::DeprecationWarning
pythonpath = src
testpaths = tests
